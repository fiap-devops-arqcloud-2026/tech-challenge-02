# ============================================================
# SECRET — flag-service
# ============================================================
# MODELO PÚBLICO: preencha somente na cópia local e não comite o valor real.
# ============================================================

apiVersion: v1
kind: Secret
metadata:
  name: flag-service-secret
  namespace: togglemaster
  labels:
    app: flag-service
type: Opaque

stringData:
  # Conexão com o PostgreSQL dedicado ao flag-service.
  DATABASE_URL: "postgres://toggle:REPLACE_WITH_DB_PASSWORD@REPLACE_WITH_RDS_ENDPOINT:5432/flags_db"
